# 🚀 افزایش زمان و حجم دیتابیس (xui-bulk)

![Python](https://img.shields.io/badge/Python-3.8%2B-blue?style=flat-square)
![Platform](https://img.shields.io/badge/Platform-Linux%20%7C%20Ubuntu-green?style=flat-square)
![License](https://img.shields.io/badge/License-MIT-orange?style=flat-square)

**ابزاری برای مدیریت دسته‌جمعی کاربران در انواع پنل‌های X-UI.**
این اسکریپت با نام `xui-bulk` روی سرور نصب شده و افزایش گروهی را انجام میدهد.

---

## ✨ ویژگی‌های کلیدی

* **🛡️ فعال‌سازی هوشمند (Smart Enable):**
    کاربر فقط در صورتی فعال (Enable) می‌شود که **همزمان** دو شرط زیر را داشته باشد:
    ۱. تاریخ انقضا در آینده باشد.
    ۲. حجم مصرفی کمتر از سقف مجاز باشد.
* **📅 تمدید زمانی شناور:**
    * *کاربر منقضی:* زمان به **لحظه حال** اضافه می‌شود (احیای اکانت).
    * *کاربر فعال:* زمان به **انتهای اعتبار فعلی** اضافه می‌شود (تمدید اکانت).
* **📦 محافظت از داده‌ها:**
    قبل از هر عملیاتی، یک **بکاپ خودکار** از فایل دیتابیس تهیه می‌شود.
* **🔄 همگام‌سازی دوگانه:**
    تغییرات همزمان در تنظیمات نمایشی (JSON) و هسته اصلی (SQL Table) اعمال می‌شود.
* **📍 اجرای آسان:**
    پس از نصب، تنها با زدن دستور `xui-bulk` در هر مسیری، برنامه اجرا می‌شود.

---

## 📥 نصب و اجرا

برای نصب، کافیست دستور زیر را در ترمینال سرور اجرا کنید:

```bash
bash <(curl -Ls [https://raw.githubusercontent.com/MrMstafa/3xui-bulk/main/xui-bulk.sh]([https://raw.githubusercontent.com/USERNAME/REPO/main/xui-bulk.sh](https://raw.githubusercontent.com/MrMstafa/3xui-bulk/main/xui-bulk.sh)))
نکته: نصب‌کننده به طور خودکار پیش‌نیازها را نصب کرده و بلافاصله وارد محیط برنامه می‌شود.📖 راهنمای سریعبعد از نصب، هر زمان نیاز داشتید فقط تایپ کنید:Bashxui-bulk
۱. انتخاب دیتابیس: به صورت خودکار شناسایی می‌شود.۲. انتخاب کاربران: انتخاب پورت خاص یا کل کاربران (All Inbounds).۳. تعیین مقادیر:* تعداد روز: برای افزودن زمان (مثلاً 30).* ترافیک (GB): برای افزودن حجم (مثلاً 10).۴. تایید نهایی: مشاهده گزارش هوشمند و اعمال تغییرات.🧠 چرا xui-bulk امن‌تر است؟سناریواسکریپت‌های معمولیxui-bulkافزایش زمان برای کاربر بدون حجمفعال‌سازی اشتباه ❌غیرفعال باقی می‌ماند ✅افزایش حجم برای کاربر منقضی شدهفعال‌سازی اشتباه ❌غیرفعال باقی می‌ماند ✅افزایش همزمان زمان و حجمفعال‌سازی کورکورانه ⚠️فعال‌سازی هوشمند ✅⚠️ سلب مسئولیت (Disclaimer)این نرم‌افزار به صورت متن‌باز (Open Source) منتشر شده است.با وجود سیستم بکاپ‌گیری خودکار، توسعه‌دهنده مسئولیتی در قبال خطاهای دیتابیس ندارد.پیشنهاد می‌شود همیشه از دیتابیس خود نسخه پشتیبان جداگانه داشته باشید.📄 لایسنس
